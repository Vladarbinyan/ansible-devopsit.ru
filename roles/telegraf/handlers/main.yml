- name: Перезапуск Telegraf
  service:
    name: telegraf
    state: restarted
    enabled: yes
  listen: telegraf-config:restart
  
- name: validate nginx configuration
  command: nginx -t -c /etc/nginx/nginx.conf
  changed_when: false

- name: reload nginx
  service: name=nginx state=reloaded
