# Unison vars

unison_configs:
  - name: sync-wordpress
    src: /var/www/wordpress
    dest: ssh://app02//var/www/wordpress
    ignore:
      - "Path .git/*"
      - "Name *.{log,tmp}"

unison_cron:
  hour: '*'
  minute: '*/3'
